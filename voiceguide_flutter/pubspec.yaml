name: voiceguide_flutter
description: A new Flutter project for VoiceGuide AI MVP.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5
  http: ^1.1.0
  flutter_tts: ^3.8.3
  audioplayers: ^6.6.0
  shared_preferences: ^2.5.5
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  geolocator: ^14.0.2

flutter:
  uses-material-design: true
